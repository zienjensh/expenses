# دليل إعداد نظام اسم المستخدم

## ✅ ما تم تنفيذه

تم تحديث النظام ليدعم تسجيل الدخول باستخدام **اسم المستخدم** بدلاً من البريد الإلكتروني.

## 📋 المتطلبات

### 1. نشر قواعد Firestore الجديدة
- افتح Firebase Console → Firestore Database → Rules
- انسخ القواعد من ملف `firestore.rules` (تتضمن قواعد `usernames` collection)
- انشر القواعد

### 2. إنشاء Index للبحث عن Username
بعد نشر القواعد، ستحتاج إلى إنشاء index:

**الطريقة 1 - تلقائي:**
- عند تسجيل الدخول لأول مرة باستخدام username، قد تظهر رسالة خطأ تحتوي على رابط لإنشاء index
- اضغط على الرابط
- انتظر حتى يكتمل إنشاء Index (قد يستغرق بضع دقائق)

**الطريقة 2 - يدوي:**
1. اذهب إلى Firebase Console → Firestore → Indexes
2. اضغط على **Create Index**
3. أدخل:
   - Collection ID: `usernames`
   - Fields:
     - Field: `username`
     - Order: Ascending
   - Query scope: Collection
4. اضغط **Create**
5. انتظر حتى يكتمل الإنشاء (قد يستغرق بضع دقائق)

## 🎯 كيفية الاستخدام

### تسجيل حساب جديد:
1. اذهب إلى صفحة تسجيل الدخول
2. اضغط "ليس لديك حساب؟ سجل الآن"
3. أدخل:
   - **اسم المستخدم**: (مثل: ahmed123) - 3 أحرف على الأقل
   - **البريد الإلكتروني**: بريدك الإلكتروني الحقيقي
   - **الاسم**: اسمك (اختياري)
   - **كلمة المرور**: 6 أحرف على الأقل
4. اضغط "إنشاء حساب"

### تسجيل الدخول:
1. أدخل **اسم المستخدم** فقط (لا حاجة للبريد الإلكتروني)
2. أدخل كلمة المرور
3. اضغط "تسجيل الدخول"

## 📝 قواعد اسم المستخدم

- **الحد الأدنى**: 3 أحرف
- **المسموح**: أحرف إنجليزية (a-z, A-Z)، أرقام (0-9)، شرطة سفلية (_)
- **غير مسموح**: مسافات، رموز خاصة، أحرف عربية
- **أمثلة صحيحة**: `ahmed123`, `user_name`, `MyUser`
- **أمثلة خاطئة**: `أحمد` (عربي), `user name` (مسافة), `user@name` (رمز)

## 🔒 الأمان

- اسم المستخدم فريد (لا يمكن تكراره)
- لا يمكن تغيير اسم المستخدم بعد الإنشاء
- لا يمكن حذف اسم المستخدم
- البريد الإلكتروني مخفي من المستخدمين الآخرين (يستخدم فقط داخلياً مع Firebase Auth)

## 🎉 ميزات إضافية

- **تحويل تلقائي**: يتم تحويل اسم المستخدم إلى أحرف صغيرة تلقائياً (Ahmed → ahmed)
- **حماية من التكرار**: النظام يمنع استخدام اسم مستخدم موجود مسبقاً
- **رسائل خطأ واضحة**: عند وجود مشكلة، ستظهر رسالة توضح السبب

---

**الآن يمكنك تسجيل الدخول باستخدام اسم المستخدم! 🚀**

